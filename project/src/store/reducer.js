const initialState = {
  genre: '',
  films: [],
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    case '':
      return {

      };
    case '':
      return {

      };
    default:
      return state;
  }
};

export {reducer};
